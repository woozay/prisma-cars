# import CarMake from "./generated/prisma-schema/prisma.graphql"

type Query {
  carMakes: [CarMake!]!
}

type Mutation {
  addCar(make: String, model: String, trim: String, zero_to_60: String, no_of_doors: String, no_of_seats: String, power_ps: Int): CarMake
}